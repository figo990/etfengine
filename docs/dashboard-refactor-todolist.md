# Dashboard 后续优化 TodoList

## 已完成

- [x] 收敛一级导航：从 13 个页面整理为 8 个工作流入口。
- [x] 旧页面迁移到 `src/dashboard/legacy_pages`，通过新入口按需加载，功能不丢失。
- [x] 首页从介绍页升级为总览页，展示市场指标、数据新鲜度、产业链热度和快捷入口。
- [x] 新增产业链研究模块，支持产业链图谱、企业趋势、重大新闻和横向对比。
- [x] 新增数据管理模块，支持手工更新、定时采集说明、数据健康检查。
- [x] 产业链企业行情支持约半年历史抓取和交易日定时更新。
- [x] 修复 legacy 页面嵌套加载时重复渲染侧栏数据管理的问题。
- [x] 新增 `data_update_runs` 数据更新日志表。
- [x] 数据刷新函数记录任务状态、写入行数、成功/失败/跳过数量和错误信息。
- [x] 数据管理页新增“更新日志”tab。
- [x] 数据更新日志支持详情查看和失败项重试。
- [x] 数据管理页增加调度器运行状态检测。
- [x] `组合中心` 原生化：组合概览、持仓配置、再平衡、风险监控整合为一个页面。
- [x] `策略实验室` 原生化：定投信号、网格设计、轮动观察、通用回测整合为一个页面。
- [x] `资讯事件` 原生化：新闻总览、政策追踪、外盘季报、预警事件整合为一个页面。
- [x] `报告中心` 原生化：接入数据新鲜度、产业链洞察、组合风险、重大新闻与外盘季报。
- [x] 产业链企业支持 `aliases` 别名，提高简称、品牌名场景下的新闻匹配准确度。
- [x] 一级页面统一宽屏布局，并按页面类型保留合适的内容阅读宽度。
- [x] 产业链企业补充营收、净利润、ROE、PE/PB 和业绩预告数据。
- [x] 资讯事件新增事件时间线与后续跟踪状态。
- [x] 策略实验室支持保存回测方案和多策略横向对比。

## P0：下一步高优先级

- [x] 把 `策略实验室` 中的定投、网格、轮动和通用回测抽成原生页面，减少 legacy 页面嵌套。
- [x] 把 `组合中心` 中的组合概览和持仓设置合成一个原生页面，避免两个旧页面切换。
- [x] 为所有数据更新任务增加可重试失败清单，支持只补采失败标的。
- [x] 为数据更新日志增加详情查看区域，展示失败代码和错误原因。
- [x] 增加调度器运行状态检测，区分“配置存在”和“进程正在运行”。
- [x] 把 `资讯事件` 的智能资讯、外盘季报抽成原生 tabs，减少 legacy 页面嵌套。

## P1：研究能力扩展

- [x] 产业链企业增加别名字段，提高新闻匹配准确度。
- [x] 产业链企业增加基本面指标：营收、净利润、ROE、PE/PB、业绩预告。
- [x] 智能资讯增加事件时间线和后续跟踪状态。
- [x] 报告中心接入产业链洞察、组合风险、重大新闻和数据新鲜度。
- [x] 策略实验室支持保存回测方案和多策略横向对比。

## P2：工程与体验

- [x] 全量清理历史 ruff 问题，统一导入顺序和页面代码风格。
- [x] 提取通用 Streamlit 组件：数据状态条、结果表、指标卡、空状态提示。
- [x] 增加 API 端数据管理接口，支持远程触发更新任务。
- [x] 减少页面级直接写库，逐步改为服务层统一执行。
- [x] 增加 DuckDB 只读连接说明和锁冲突提示。

## 本轮补充

1. 新增 `src/dashboard/services.py`，统一承接组合配置、回测方案和资讯跟踪状态的持久化入口。
2. 新增 `src/dashboard/components.py`，抽出指标卡、数据状态条、结果表和空状态提示。
3. Legacy 页面与全仓代码已通过 Ruff 校验，历史导入顺序和长行问题已清理完成。

## 可靠性优化

- [x] 增加数据健康阈值配置，区分行情、新闻、财务、业绩预告和外盘季报的新鲜度要求。
- [x] 增加近端日线断档检测，覆盖 ETF 行情、产业链企业行情和企业估值。
- [x] 自动生成补采建议，并在数据管理页支持按建议执行可自动任务。
- [x] 调度器新增数据健康检查任务，并在任务异常或健康异常时推送通知。
- [x] 数据管理 API 新增 `/health`，统一返回新鲜度、断档、最近失败与补采建议。
- [x] 增加首页与报告中心的 Streamlit 页面冒烟测试，减少重构后的人工巡检成本。

## 后续可继续推进

- [x] 将新闻监控和外盘季报也纳入统一的手工补采入口，补齐当前“只能提示、不能一键执行”的两类任务。
- [x] 为补采任务增加幂等队列或后台任务状态，避免长任务阻塞页面。
- [x] 逐步把旧页面中的 `use_container_width` 升级为新的 `width="stretch"` 接口，提前消除 Streamlit 兼容性告警。
- [x] 完成 Streamlit 架构升级评估，结论为“保留 Streamlit，继续做分层与任务化升级”。

## 新一轮建议

- [x] 将后台任务状态从进程内内存升级为 DuckDB 持久化表，支持重启后查看任务历史。
- [x] 将旧 legacy 页面继续原生化，优先处理 `估值与市场` 和 `产业链研究` 两个高频入口。
- [x] 把数据采集/补采类长任务按钮统一切换为后台任务提交，并在侧栏提供全局任务状态入口。

## 本轮完成

1. 新增 `dashboard_tasks` DuckDB 表，后台任务状态、结果和错误信息会持久化；进程重启后未完成任务会自动标记为 `interrupted`。
2. `估值与市场` 已替换 legacy 嵌套，原生展示市场温度、指数估值、股债性价比和历史走势。
3. `产业链研究` 已替换 legacy 嵌套，原生展示图谱、企业趋势、经营质量、重大新闻、横向对比和数据覆盖。
4. 侧栏新增全局后台任务状态入口；数据管理、组合中心、资讯事件和产业链研究中的采集/补采按钮统一提交后台任务。
5. 页面冒烟测试覆盖 `首页`、`估值与市场`、`产业链研究`、`报告中心` 和 `数据管理`。

## 下一轮可选优化

- [x] 把策略回测、报告生成等计算型长任务也纳入后台任务，并为结果增加专门的回看/下载区域。
- [x] 为 `dashboard_tasks` 增加保留周期配置，定期清理过旧成功任务，保留失败和中断任务用于排查。
- [x] 为数据管理 API 增加后台任务提交与任务查询接口，便于远程触发后轮询状态。

## 本轮补充完成

1. `策略实验室` 的通用回测改为后台任务提交，新增“回测结果”tab，支持查看指标、收益曲线、成交记录、下载 JSON，并可将完成的结果保存为方案。
2. `报告中心` 的报告生成改为后台任务提交，新增“生成任务”tab，支持查看生成状态、预览 Markdown 和下载报告文件。
3. `dashboard_tasks` 增加成功任务保留期配置：默认成功任务保留 30 天；失败和中断任务不随定期清理删除，用于后续排查。
4. 调度器新增 `dashboard_task_cleanup`，按 `dashboard_task_cleanup_time` 定期清理过旧成功任务。
5. 数据管理 API 新增：
   - `POST /api/data-management/tasks`：提交后台数据刷新任务；
   - `GET /api/data-management/tasks`：查询后台任务列表；
   - `GET /api/data-management/tasks/{task_id}`：查询单个任务状态与结果。

## 后续可选优化

- [x] 为报告生成增加定时周报/月报配置，自动生成后接入通知推送。
- [x] 为回测任务增加参数模板和批量组合回测，统一进入后台队列。
- [x] 为后台任务增加标签/类型字段，避免仅依赖 `task_key` 前缀筛选。

## 本轮继续完成

1. `dashboard_tasks` 增加 `task_type` 与 `tags` 字段；旧任务会从 `task_key` 兼容推断类型，新任务显式写入类型和标签。
2. 调度器新增自动周报/月报任务：
   - `weekly_report_generation`：按 `weekly_report_day` + `weekly_report_time` 生成周报；
   - `monthly_report_generation`：每月最后一天按 `monthly_report_time` 生成月报；
   - 报告生成完成后通过已配置通知渠道推送文件路径和大小。
3. `报告中心` 的自动报告配置展示已从“待接入”更新为真实配置状态。
4. `策略实验室` 新增“批量回测”tab，内置普通定投、MA 偏离定投、等差/等比网格模板；批量任务统一进入后台队列，并在“回测结果”查看。
5. 数据管理 API 的任务列表支持按 `task_type` 筛选，任务返回包含 `task_type/tags`。

## 后续精细化优化

- [ ] 为后台任务增加更细的任务结果索引表，避免大结果长期存放在单行 JSON。
- [ ] 为批量回测增加任务组 ID，支持一组任务的整体进度、汇总排序和一键保存方案。
- [ ] 为自动报告增加页面化开关和时间编辑，写回配置文件。
